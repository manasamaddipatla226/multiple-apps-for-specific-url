from django.shortcuts import render
from django.http import HttpResponse
def adrenaline(request):
    return HttpResponse('adrenaline is the hormone which releases at the time of angry')

# Create your views here.
